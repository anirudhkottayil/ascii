# pyproject.toml
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "img2asc"
version = "0.1.0"

[project.scripts]
img2asc = "img2asc.cli:main"

[tool.setuptools.package-data]
img2asc = ["bin/*"]
